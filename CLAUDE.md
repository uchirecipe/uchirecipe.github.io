# うちレシピ 開発ルール（全モデル・全セッション共通）

自分専用レシピ帳PWA。設計書は `../docs/`。**着手前に必ず `docs/06_開発バックログ.md`（今後のキュー・既知事象・却下記録）を読む**。大きな判断・オーナー対応・レシピ制作の前は `docs/17_Fable引き継ぎ書.md`（オーナー解釈の判例集・既定値裁定）も読む。仕様に迷ったら 02_要件定義 → 04_技術設計。機能実装の進捗は `docs/09_開発ロードマップ.md`、収益・価格・広告・決済・法務は `docs/08_収益化ロードマップ.md`（現行の正）。直近の決定は `docs/README.md` の決定事項ログ。

**経緯・失敗の実例など詳しい説明は本ファイルの旧版全文 `../docs/_archive/2026-07-27/CLAUDE.md` を参照。**

## ユーザー
プログラミング超初心者。報告は日本語・専門用語は噛み砕く。大きな設計変更・お金が絡む判断は勝手にせず必ず確認する。

## 相談先とエスカレーション
- **重要判断の相談先は「そのとき使える最上位モデル」**。**2026-08-03よりFableが当面使えないため、司令部（裁定・設計・オーナー対応・便の統合）はOpusが務める**（`model: "opus"`。実装便もOpus、量産・調査・ペルソナはSonnet）。Fableが復帰したら `model: "fable"` に戻す。引き継ぎの現在地と手順は `../docs/17_Fable引き継ぎ書.md` の §0。
- 判断の優先順位: ①ユーザーの指示（既定値と矛盾したらユーザーに従い docs/08 とログ更新）②docs/08 の既定値・権限区分（**勝手に再発明しない**）③決まらなければ上位モデル相談。
- **上位モデル相談を使う条件**: (1)要望が曖昧で複数解釈 (2)要望が既存の設計・決定と矛盾/変更を迫る (3)同じバグ修正に2回失敗 (4)ユーザーが「相談して」（無条件）(5)docs/08に既定値のない収益・戦略判断 (6)docs/09でOpus/Fable推奨のバッチ（並行調理ナビ・栄養価概算・ストア化）。**選択肢2〜3個＋推奨1つ＋理由**を返させ最終決定は必ずユーザー。
- 作法: ユーザー発言は要約せず**原文のまま**渡す／試したことと結果（エラー原文）／関係ファイルパス／docs該当箇所。追加質問は新エージェントでなく**SendMessageで同じエージェントに返信**。決定はREADME決定ログか docs/08 に記録。**上位モデルの推奨は聞かれていない部分も含め実装/後回し/却下を毎回もれなく報告**。

**ユーザー確認が必須（どのモデルも勝手に進めない）**:
- お金: 価格の決定・変更、販売の開始/停止、支出（ドメイン等）、アフィリエイト導入
- 対外公開: note/X投稿の公開、LP/規約/プラポリ/特商法の新規公開や大幅変更
- 後戻りしにくいもの: 既存無料機能の有料化・削除、データを失いうる変更、**登録件数制限のON（FREE_LIMIT_ENABLED）と上限件数（FREE_LIMIT）の変更**
- レシピ内容の最終確定（管理栄養士ペルソナQAを通した上でユーザー承認）

**月次レビュー**: 毎月初、計測数値・売上・ご意見箱を docs/09 のマイルストーンと照合して報告。要判断は上位モデルへ。

## 解釈・実装の運用ルール（規約A〜H・要約。詳細と背景は_archive参照）
- **A 結合テスト**: 句点をまたいで結合すると自然な一文になる断片（例「レシピ順」）は単体で独立要望にしない。非可逆・データ設計に関わるものは確認、可逆で軽微なら自然な方で実装し**完了報告の冒頭に解釈メモを明示**。
- **B 指示は字義どおり**: 位置・文言・色など具体的指示＋理由が併記されたら指示を字義どおり実装。理由文から「より良い案」を推論して上書きしない。
- **C 2択提示は委任**: 「AかBか」は確認不要でSonnetが選ぶ（①可逆・非破壊 ②既存機能を妨げない）。選ばなかった案と理由を1行残す。
- **D 固定コンテンツ（スターターレシピ等）の文面編集**: 具体指示があればSonnet単独可。ただし①変更後の文面を完了報告に原文で載せる②基本手順は変えずmemo追記優先③指示のない調理知識の新規書き下ろし、加熱・衛生・保存に関わる変更は上位モデル相談必須。
- **D-④ 安全注記チェックリスト**（新規レシピ・セット制作時に該当したら必ず入れる。既存への遡及一括はしない。詳細な理由と文例は_archive／recipe-set-productionスキル）: ①肉・卵・魚の加熱=火通り目安を該当stepのmemoに ②**厚み不均一な部位（鶏むね塊）・骨付き肉は竹串/汁の色で判断せず、一番大きい1個を切り開いて赤みがなければOK・赤ければ1分ずつ追加**（温度計は書かない）③半熟=お子様/高齢者/妊娠中への完全加熱案内 ④ルー物/煮物/汁物4人前以上or作り置き=早めの冷蔵＋食前の沸騰再加熱 ⑤作り置き副菜=保存日数明記 ⑥お弁当=完全に冷ましてから／卵・肉は完全加熱 ⑦はちみつ=1歳未満に与えない（必須）。交差汚染注意も。文体=簡潔な常体1〜2文（必須「〜こと」/推奨「〜と安心」・菌名/恐怖/効能誇大は禁止）。置き場所=doneness→該当stepのmemo（`s(text,minutes,memo)`第3引数）/保存・再加熱・対象者・弁当・交差汚染→レシピ本体のmemo。**事後追記でstepのtext本文は変えない**。
- **E バッチ運用**: ①「全体的に◯◯前提で」等の枠組み変更指示は既存**手順本文まで全文掃引**し、複数品への一括適用前に**1品だけ直した見本で方向確認** ②完了報告に「localhost:5173を再読み込みして確認」を添え見る場所（URL/パス）を明記 ③デザイントークン分割時は旧トークンの全使用箇所を掃引 ④外部従量リソース（Codex等）は消費モデルを先に確定 ⑤**サブエージェント（worktree便）に「ユーザーのdevサーバー(5173)を落とさない・killは自分のポート/PID限定」・ブラウザ(chromium)の一括pkillも禁止(並行便のe2eを巻き込む事故が2026-08-02実発)」を必ず明記**し、便に渡す指摘は現状コードで解消済みでないか検証して済みは除外。
- **F 破壊的操作の確認文**: 「**何が消えるか**」と「**何が残るか**」を必ず両方書く（件数も）。「よろしいですか？」だけは禁止。
- **G 書き溜めバッチへの最終対応表**: まとめて投げられた意見に対応したら、**最後に必ず全項目の対応表**（実装/却下＋理由/代案/保留＝誰待ち）を1返信で。載っていない項目を作らない。 対応表には必ず**「CEO評価」列（Fable不在時も同じ。旧称「Fable評価」）(賛成/条件付き/懸念あり+一言)**を付け、鵜呑みでなく裁定した痕跡を毎回見せる(2026-08-02オーナー指摘)。
- **H ユーザー向け文言の作法**: オーナーの内部向け表現（「おまけ」「とりあえず」「雑に」等）をUI・対外文書にそのまま流さない。**機能を卑下する表現は全面禁止**。UI文言は「読んで機能の性質・使い方が分かるか」だけで書く。便にオーナー原文を引用するときは「原文は意図の説明・UI文言は書き直す」を添える。 **司令部が便のプロンプトに書いた文言も「案」にすぎない**。便は必ず規約Hで精査して最終文言を決め、**採用した文言を報告に原文で載せる**(2026-08-09オーナー再指摘「アプリ内に実装してユーザーの目に触れる文字は、必ず精査して決定して。私の話し言葉の指示をそのまま使わないで。何度も同じ指摘しています」)。 説明文・ヘルプでは「ここ」「これ」等の指示語で場所を示さない(画面名・ボタン名で言う。2026-08-02オーナー指示)。 **余計な一言の禁止**: 機能説明に「〜のために作った」等の開発意図・気の利いた飾り文・自明な強調(「すべて」等)を足さない。数字や事実が語る部分は省く(2026-08-02オーナー指示「気の利いたひと言を入れようとして余計なひと言が入る傾向」)。長文は15行以上黒文字が続かないよう分割・折りたたみ・表で構成する。
- **オーナーへの質問の作法**: 「要判断」とだけ書かない。必ず「A案/B案＋推奨1つ＋理由」の形で、画面にどう出るかの具体例つきで聞く。オーナーの返信が質問形なら確定扱いにせず、具体例で読み合わせてから進める(2026-08-03オーナー指摘「何が確定したのですか？怖い」)。
- いずれも「確認過多」を目的としない。バッチ運用（実装→検証→報告→違えば次バッチで訂正）の速さを殺す過剰な事前確認はしない。

## 環境
- Node.js は `~/.local/node`。**各Bashコマンドの先頭に必ず** `export PATH="$HOME/.local/node/bin:$PATH" && ` を付ける。
- スタック: React + TypeScript + Vite + Tailwind v4 + Dexie(IndexedDB) + vite-plugin-pwa + lucide-react。
- devサーバー: `npm run dev` → http://localhost:5173/（ユーザーが開いていることがある。**勝手に落とさない**）。

## コーディング規約
- UI文言はすべて `src/i18n/ja.ts` に集約（ハードコード禁止）。
- 色は `src/index.css` のデザイントークン（CSS変数）のみ。直接色指定禁止。文字用アクセントは --accent-ink-page／--accent-ink-surface の2本を持ち、実際に使う --accent-ink はカード面を塗るクラス(bg-surface/bg-app)内で自動で切り替わる(ブラウンのみ面で値が異なる #833a00/#ad4e01)。カラーテーマは 自動/ライト/ダーク/ブラウン/グリーン。
- **アクセントは「塗り」と「文字」で別トークン**（2026-07-30 配色AA調整）。**文字色には `text-accent-ink`（`--accent-ink`）を使う**（`text-accent`は廃止）。塗り・枠・リング・アイコンは従来どおり `bg-accent` / `border-accent` / `ring-accent` / `var(--accent)`。ライトのみ文字用が濃い（#b8380a）で、他テーマは `--accent-ink: var(--accent)` と同値。`public/about/*.html` は同じ色定義を各ファイルが書き写しているため、色を変えるときは全ファイル(現在10・foods.htmlは生成物なので直す先は scripts/gen-food-price-page.mjs)に同じ値を入れる。
- アイコンは lucide-react のみ。**例外**: レシピ料理カテゴリのプレースホルダー（`RecipeIcon`）は `public/icons/` のカスタムPNGをCSSマスクで描画しテーマ色（`var(--accent)`）に追従。
- スマホ縦画面基準・料理中に使う前提でボタン大きめ。
- ルーティングは HashRouter。**basenameは付けない**（付けると白画面。事故済み）。
- 新フィールドは任意項目にして既存データを壊さない（マイグレーション不要を保つ）。

## ブランチ運用
- **`dev`が作業用の既定ブランチ**（GitHubのdefaultも`dev`）。日常の開発・コミット・pushは`dev`。`dev`へのpushは本番に影響しない（deploy.ymlは`main`のpushのみ反応）ので気兼ねなくpushしてよい。
- **`main`は「本番にそのまま出す」意図のときだけ**。リリース時は`dev`→`main`マージ→`main`push（このpushが本番デプロイのトリガー）。
- 新セッション開始時、まず`git branch`で`dev`にいることを確認。
- **本番の準備中ページ表示は `MAINTENANCE_MODE`（`src/logic/maintenance.ts`）で制御**。うかつにdev→mainマージするとfalseが混入し意図せず再公開されうる。**再公開・発売はユーザーの明示指示があるまで行わない**。

## 検証とコミット
- 完了条件: `npx tsc -b` エラーゼロ ＋ `npm run build` 成功 ＋ **`npm test`**（ロジック単体・栄養回帰・レシピlint一括）。UIに触れた変更は **`npm run test:e2e`**（恒久Playwrightスモーク）も通す。
- **e2eの対象指定は環境変数 `BASE_URL`**（例: `BASE_URL=http://localhost:4173 node scripts/e2e-smoke.mjs`）。`E2E_BASE_URL`等の誤名は無視されデフォルト5173（オーナーdev・不可侵）に向かう事故が起きた。スクリプトに5173ガード（`ALLOW_DEV_SERVER=1`で解除）と対象URL表示あり。5173はvite devでSW無し・静的ページのディレクトリURLがSPAシェルに化けSMK-19が偽陽性で落ちる点に注意。
- **バグを直すときは、再発防止ケースを`scripts/test-logic.mjs`に足してから直す**（docs/10 3章）。
- コミットメッセージは日本語（`main`push=本番公開の詳細はブランチ運用参照）。

## 収益設計の前提（現行の正。最新・詳細は docs/08 が一次参照）
- **課金モデル=全品無料＋機能の買い切り**。収録レシピ（同梱の基本レシピ・現在109品）は全て無料（テーマ・第◯弾の括りは廃止）。有料は**買い切り版800円（早期）→正式1,500円（税込）**の単一商品で、**機能のみ課金**＝①登録レシピ上限解除（無料30件→無制限）②栄養8項目表示（食塩相当量を含む）＋栄養での並び替え ③月間献立 ④並行調理ナビ。サブスク禁止・追加課金なし。旧「500円追加レシピパック／Pro凍結／UP-コード」は廃止。
- **栄養の無料/有料の線引き（2026-08-01 オーナー確定・線引きB'）**: 無料＝**エネルギー（kcal）＋野菜量(g)** と**カロリー順の並び替え**まで。Pro＝**栄養8項目（食塩相当量を含む）**＋野菜量、**たんぱく質・塩分・脂質・糖質順の並び替え**、食塩相当量のめやすとの並置。野菜量とそのめやすは無料のまま（バランス提案エンジンが使う基準なので、無料でも見えないと選定理由を説明できない）。バランス提案エンジン自体は無料・既定オン（docs/60）。**エンジンの内部計算は無料/Proで同一で、変えているのは表示のゲートだけ**。
- 無料版=自分の登録30件まで（`isStarter=true`の同梱レシピは数えない。**2026-08-08 オーナー決定で50→30**・宣伝開始前のため線引き変更可。アンケート記載と一致させた）。**制限ON（`FREE_LIMIT_ENABLED`）は買い切り版の発売と同一リリース**（それまで false）。新規追加のみブロックし、既存の閲覧・編集・削除とバックアップ復元は絶対に制限しない。登録件数の案内は**20件目・27件目・30件目の登録完了時だけ**（常時表示しない・オーナー指示）。無料のお試しは**ナビ3回・月間献立1回・栄養8項目1回**。
- **バナー広告は導入しない**（規模で割に合わず・UX毀損。再検討は docs/08 4章）。
- 解錠はオフライン検証のコード方式（`UR-`）。**コード原本 `private/pro-codes-master.txt` はリポジトリに絶対コミットしない**（アプリに載せるのはハッシュのみ）。
- 決済は**Stripe（Payment Link・Managed Payments）で確定**。決済完了→Cloudflare Worker（`workers/purchase-fulfill/`）がその場で解錠コードを表示。特商法表記は独自ドメイン `/about/tokushoho.html`（実装済み・本番未コミット＝本名の公開は本番デプロイと同時）。旧「BOOTHも可」は不採用。
- AI機能はサブスク禁止。写真取り込みはBYO-AI（ユーザー自身のAIでJSON化→貼り付け）。
- 個人データをサーバーに送らない（端末内保存）。計測は匿名アクセス統計（Cloudflare Web Analytics）のみ・プラポリに明記済み。
- **URLは https://uchirecipe.com/ 。以後オリジン変更は禁止**（端末内データが見えなくなる）。

