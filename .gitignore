# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
dev-dist
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 栄養価概算の元データ(文科省公式Excel、build-nutrition.mjsが自動DLするキャッシュ)
scripts/data/*.xlsx

# レビュー用レシピセット(docs/12原稿からscripts/build-review-pack.mjsが生成する一時物。
# 本番に混入させないためコミットしない)
public/sets/data/review.json

# workers/recipe-import(URL取り込みWorker): wranglerのローカル実行キャッシュ・秘密情報ファイル
workers/recipe-import/.wrangler
workers/recipe-import/.dev.vars*
.wrangler
dist-urlimport-e2e

# workers/purchase-fulfill(購入後コード自動配信Worker): wranglerのローカル実行キャッシュ・秘密情報ファイル
workers/purchase-fulfill/.wrangler
workers/purchase-fulfill/.dev.vars*

# 使い方ページのスクショ撮影で使うデモ用の料理写真(ぱくたそ・再配布しないためコミットしない)
.manual-photos/

# 月間デモの写真の原本(ぱくたそ・再配布しないためコミットしない。
# コミットするのは scripts/build-demo-photos.mjs が作る public/demo/*.webp だけ)
.demo-photos/

# 実機で試すためのテスト用レシピの束(オーナーの手元にだけ置く。取り込み元のサイトの
# レシピ本文・写真が入っているので、リポジトリにも配るものにも入れない。.manual-photos と同じ扱い。
# 置き場所は ../テスト用データ/ でリポジトリの外だが、うっかり中へ写しても入らないようにしておく)
うちレシピ_テスト用データ*.json
テスト用データ/

# 節だけ切り出して走らせる道具の一時ファイル（2026-08-23 便JMの申し送り3）。
# 実行が途中で止まると残り、うっかりコミットに混ざる
scripts/e2e-part.tmp.mjs
