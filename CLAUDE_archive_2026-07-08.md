# CLAUDE.md 整理退避（2026-07-08）

CLAUDE.md整理で書き換え・移動した行の**原文をそのまま**保存するファイル。完全な整理前の姿は `CLAUDE_backup_2026-07-08.md` 参照。各行の後の（→）は退避理由。

## 冒頭(L3)の原文

自分専用レシピ帳PWA。設計書は `../docs/`（01企画〜09開発ロードマップ）にあり、**着手前に必ず docs/06_開発バックログ.md を読むこと**。仕様判断に迷ったら 02_要件定義.md → 04_技術設計.md の順で確認。**アプリ開発（機能実装）のマイルストーン・進捗は docs/09_開発ロードマップ.md**、**収益化・価格・広告・決済・法務等の意思決定は docs/08_収益化ロードマップ.md** が一次参照文書（Fableの相談窓口がなくなった後の既定値集。2026-07-05に開発進捗と収益意思決定を別文書に分離）。

（→ docsの範囲表記が古かった(実際は01〜13)ため更新。「分離した」経緯は行動を変えないため退避）

## 相談先とエスカレーション節の見出しとFable経緯(L9・L11)の原文

## 相談先とエスカレーション（2026-07-06訂正: Fableは現在利用可能）

**Fableは現在も利用可能**（2026-07-04時点で「あと3日で使えなくなる」と予告されていたが、2026-07-06時点でユーザーが利用可能と確認。使えなくなったらユーザーから連絡が来る、またはサブエージェント起動時のエラーで気づけるはずなので、それまでは下記の相談にAgentツールで**model: "fable"**を使うこと（"opus"ではない）。使えなくなった場合のみ、下記の条件・作法はそのままにmodel:"opus"へ切り替える。収益化・戦略の既定値は **docs/08_収益化ロードマップ.md** にFableが策定済み。判断の優先順位:

（→ 利用可否の経緯4行に指示が埋没していたため、「そのとき使える最上位モデル(現在はfable、不能時のみopus)」という恒久形に書き換え=2026-07-08ユーザー裁定）

## 相談の作法(L25)の原文

相談の作法: ユーザー発言は要約せず**原文のまま**渡す／試したことと結果（エラー原文）／関係ファイルパス／docsの該当箇所を添える。追加質問には新しいエージェントを立てず**SendMessageで同じエージェントに返信**して対話を続ける。決まったことは docs/README.md の決定事項ログか docs/08 に必ず記録する。**Fableの推奨内容は、ユーザーから聞かれていない部分も含めて、実装したか・後回しにしたか・却下したかを毎回もれなく報告する**（2026-07-06、報告漏れをユーザーに指摘され徹底することになった）。相談の回数・トークン量は気にせず、重要な判断は都度相談してよい。

（→ 末尾の経緯括弧のみ退避、ルール本体は新版に維持）

## 月次レビュー(L33)の原文

**月次レビュー（Fableのフェーズ完了レビューの代替）**: 毎月初、計測数値・売上・ご意見箱をSonnetが表に整理し、docs/09_開発ロードマップ.mdのマイルストーンと照合して「順調/遅れ/要判断」を報告。要判断はOpus相談へ。手順詳細は docs/09 の「定常運用」参照。

（→ 「Opus相談へ」はFable不在前提の古い記述。「そのとき使える最上位モデルへ」に統一=2026-07-08ユーザー裁定）

## 運用ルール導入文(L36)の原文

音声入力フィードバックの解釈でミスが2件（曖昧な断片を誤って独立要望と解釈／位置指示より理由文の推論を優先）あったため、再発防止として以下を運用する。

（→ 事故の内訳は経緯。ルールA〜Dは新版に維持）

## ブランチ運用の将来展望(L71)の原文

- この運用は無料版の「非公式リリース状態での開発」だけでなく、**将来Pro機能を追加開発するときも同じ形で使う**（`dev`で作り込み、レビュー後に`main`へマージして公開）。あわせて`FREE_LIMIT_ENABLED`等の既存フラグ方式（コードは`main`にあっても機能自体はフラグで隠す）と併用すると二重に安全

（→ 将来の展望・設計思想であり現在の行動を変えないため退避）

## 検証とコミットのmain push説明(L77)の原文

- **`main`へのpush = 本番公開**（GitHub Actionsが https://uchirecipe.com/ へ自動デプロイ）。動作検証が済み、実際にリリースしたい変更だけを`main`にマージ・pushする。`dev`へのpushはこれに該当しない

（→ ブランチ運用節(L68-69)と二重記載だったため、末尾の重複文を削って統合）

## 収益設計の前提 節(L79〜L86)の原文（2026-07-04 Fable最終改訂版・全文）

## 収益設計の前提（実装に影響する決定事項・2026-07-04 Fable最終改訂）
- 無料版=自分の登録50件まで（isStarter=true のスターター・配布セットは数えない）。制限はPro購入手段の公開と同一リリースでON（それまで FREE_LIMIT_ENABLED=false で寝かせる）。新規追加のみブロックし、既存データの閲覧・編集・削除とバックアップ復元は絶対に制限しない
- **バナー広告は導入しない**（技術的な審査可否ではなく、想定規模での収益がごく小さくUX毀損に見合わないため。独自ドメイン取得後も結論は同じ。詳細と再検討条件は docs/08 2-3-B）
- Pro=買い切りのみ（先行500円→大型更新時800円、既購入者据え置き）。解錠はオフライン検証のコード方式（docs/08 2-6。コード原本はリポジトリに絶対コミットしない）
- AI機能はサブスク禁止。AI回数券は無期限保留（自前サーバーを持つ判断をした場合のみ再検討）。写真取り込みはBYO-AI方式（ユーザー自身のAIでJSON化→貼り付け）で代替
- 個人データをサーバーに送らない（端末内保存）。計測は匿名アクセス統計（Cloudflareビーコン）のみで、これはプラポリに明記済みであること
- **URLは https://uchirecipe.com/ に切替済み**（2026-07-05、M1-0完了。旧`uchirecipe.github.io`は301で自動リダイレクトされる）。**これ以降オリジン変更は禁止**（端末内データが見えなくなる）。詳細はdocs/08 2-3-A
- 決済処理そのものはアプリ外（Stripe推奨・BOOTHも可）で完結させる。特商法表記・Pro紹介ページは独自ドメイン上（Stripe選択時、`/about/`配下）またはBOOTH側（BOOTH選択時）のどちらでもよい（docs/08 2-5・2-7、2026-07-05推奨変更）

（→ 2026-07-06決定=Pro発売凍結・M4は2商品構成・パック500円確定・50件制限はパック販売と同時ON・Pro価格白紙見直し、と食い違うため最新決定に更新=2026-07-08ユーザー裁定。「Pro=500円→800円」「制限はPro購入手段の公開と同一リリースでON」は特に古い）
